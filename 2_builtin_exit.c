#include "shell.h"

/**
 * builtin_exit - exit the funct
 * @command: the function
 * @tokens: an array of the function and the arguments of the functions
 * Return: zero if sucess, or other number if its declared in the arguments
 */

int builtin_exit(token_node **tokens)
{
/*	int counter;

walk through the array of tokens
	for (counter = 0; (*tokens) != '\0'; counter++)
	{
		printf("token %d:%s\n", counter, tokens[counter]);
	}
	printf("counter:%d\n", counter);
*/
	return (0);
}
